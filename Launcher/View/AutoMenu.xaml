<UserControl x:Class="Launcher.View.AutoMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Loaded="Window_Loaded">
    <Grid Margin="0,0,0,0">
        <Grid MinHeight="600">
            <Grid.RowDefinitions>
                <RowDefinition Height="300" />
                <RowDefinition Height="100" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <ListView ItemsSource="{Binding ListDevices}"
                      SelectedItem="{Binding SelectedDevice, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"
                      Background="#FFA5A4A4"
                      Margin="0,-80,0,0" MaxHeight="300" MinHeight="300" Grid.RowSpan="2">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="White" />
                        <Setter Property="Foreground" Value="Black" />
                        <Setter Property="BorderBrush" Value="Gray" />
                        <Setter Property="BorderThickness" Value="1" />
                        <Setter Property="Height" Value="30" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn>
                                <GridViewColumn.Header>
                                    <Button Content="Chọn tất" Click="OnBtnAllClick" Padding="5,1,5,1" />
                                </GridViewColumn.Header>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                                            <TextBlock Text="{Binding VmIndex}" Margin="5,0,0,0" />
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>

                            </GridViewColumn>
                            <GridViewColumn Header="Loại" DisplayMemberBinding="{Binding DType}" />

                            <GridViewColumn Header="Thiết Bị"
                                            DisplayMemberBinding="{Binding DeviceName}" />
                            <GridViewColumn Header="Trạng thái" DisplayMemberBinding="{Binding Stage}" />
                            <GridViewColumn Header="Thông tin" Width="250" DisplayMemberBinding="{Binding Status}" />
                            <GridViewColumn Header="Trạng thái luồng" Width="1" DisplayMemberBinding="{Binding TStage}" />
                            <GridViewColumn Header="Hành Động" Width="155">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Chạy" Click="ButtonStart_Click" Background="#4dbd74"
                                                    Foreground="White" Padding="5,0,5,0" />
                                            <Button Content="Dừng" Padding="5,0,5,0" Margin="10,0,0,0"
                                                    Background="#f64846" Foreground="White"
                                                    Click="ButtonStop_Click" />
                                            <Button Content="Xem" Padding="5,0,5,0" Margin="10,0,0,0"
                                                    Click="ButtonScreenshot_Click" />
                                            <Button Content="TEST" Padding="5,0,5,0" Margin="10,0,0,0"
                                                    Click="ButtonTest_OnClick" />
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                        </GridView.Columns>
                    </GridView>
                </ListView.View>
            </ListView>
            <Grid Grid.Row="1" Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Content="Dừng các thiết bị đã chọn" Grid.Column="0" Click="BtnStopAllClick"
                        Background="Firebrick"
                        Height="30" Margin="3,13,3,3" FontWeight="Bold" FontSize="20" />
                <Button Content="Xem toàn bộ cửa sổ" Grid.Column="1" Click="Button_MirrorAll_Click" Background="Snow"
                        Name="ButtonSupport" Height="30" Margin="3,13,3,3" FontWeight="Bold" FontSize="20" />
                <Button Content="Chạy các thiết bị đã chọn" Grid.Column="2" Click="BtnRunAllClick"
                        Background="Chartreuse"
                        Height="30" Margin="3,13,3,3" FontWeight="Bold" FontSize="20" />
            </Grid>
            <Grid Grid.Row="2" Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>

                </Grid>
                <Grid Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                </Grid>
                <Grid Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
